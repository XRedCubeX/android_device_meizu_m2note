service cameraserver /vendor/bin/cameraserver
    class main
    user cameraserver
    group audio camera input drmrpc media vendor
    ioprio rt 4
    writepid /dev/cpuset/camera-daemon/tasks /dev/stune/top-app/tasks
